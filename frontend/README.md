# Frontend — Phase 6 (not built yet)

Will host either the Salesforce Lightning Web Component (Opportunity panel
with Start/Resume/Approve actions) or a lightweight React UI, depending on
which is faster to demo reliably. Decision deferred until the golden path
(Phases 1-5) works end to end against `adk web`'s built-in dev UI, which is
sufficient for internal testing until then.
